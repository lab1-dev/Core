export enum Anchor {
    NotSet = 0,
    Left = 1 << 1,
    Right = 1 << 2,
    Top = 1 << 3,
    Bottom = 1 << 4,
    HorizontalCenter = 1 << 5,
    VerticalCenter = 1 << 6,
    // TopLeft,
    // TopRight,
    // BottomLeft,
    // BottomRight,
    // TopCenter,
    // BottomCenter,
    // LeftCenter,
    // RightCenter
}
